-- | Node.js HTTP Server FFI bindings
module Bridge.FFI.Node.Http where

import Prelude
import Bridge.FFI.Node.Express (HttpServer)

-- | Re-export HttpServer type
type HttpServer = HttpServer
