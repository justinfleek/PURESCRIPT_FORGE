"use strict";

/**
 * OAuth Provider FFI
 * Provides OAuth URL encoding utilities
 */

// | Encode URI component
exports.encodeURIComponent = function (str) {
  return encodeURIComponent(str);
};
