{
  "name": "opencode-sidepanel-plugin-ps",
  "version": "0.1.0",
  "description": "OpenCode plugin in PureScript",
  "main": "output/Opencode.Plugin.Main/index.js",
  "peerDependencies": {
    "@opencode-ai/plugin": "^0.x"
  },
  "dependencies": {
    "ws": "^8.x"
  },
  "scripts": {
    "build": "spago build",
    "watch": "spago build --watch"
  }
}
